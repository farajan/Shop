<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="100dp"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:orientation="vertical"
    android:weightSum="1">

    <ImageView
        android:id="@+id/icon"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginStart="20dp"
        android:layout_marginTop="12dp"
        app:srcCompat="@drawable/list_default_logo" />

    <TextView
        android:id="@+id/simpleListName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/icon"
        android:layout_marginStart="21dp"
        android:layout_toEndOf="@+id/icon"
        android:text="List name"
        android:textColor="#000"
        android:textSize="18sp" />

    <TextView
        android:id="@+id/simpleListNode"
        android:layout_width="300dp"
        android:layout_height="60dp"
        android:layout_alignStart="@+id/simpleListName"
        android:layout_below="@+id/simpleListName"
        android:text="Tak trosku nejhaka debilno poznamka nebo popisek aby to vzpadalo aspon trosku cool. no co, uz nevim co dal psat, tak asi konec" />

</RelativeLayout>